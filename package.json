{
  "name": "spinal-browser-viewer-annotations",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "dependencies": {
    "spinal-organ-forge": "git+https://github.com/spinalcom/spinal-organ-forge.git"
  },
  "scripts": {
    "models": "browserify ../spinal-lib-forgefile/forgefile.js -o www/js/browser.forgefile.js -t [ babelify --presets [ es2015 ] ] -g windowify -g uglifyify"
  }
}